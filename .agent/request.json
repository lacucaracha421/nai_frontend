{
  "id": "retry-tag-library-final-20260826-1111-01",
  "enabled": true,
  "shell": "powershell",
  "cwd": ".",
  "timeout_seconds": 1800,
  "commands": [
    "$env:Path = $env:Path + ';' + [Environment]::GetEnvironmentVariable('Path','User'); $ProgressPreference='SilentlyContinue'",
    "git fetch origin agent/tag-library-backup; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}; git reset --hard origin/agent/tag-library-backup; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}",
    "python scripts/build-sexual-tags.py; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}",
    "$payload = Get-Content public/data/sexual-tags.json -Raw | ConvertFrom-Json; Write-Host ('SEX_TAG_COUNT=' + $payload.tags.Count); Write-Host ('MIN_POST_COUNT=' + $payload.minPostCount); if($payload.minPostCount -ne 1000){ throw 'Unexpected Sex tag threshold' }",
    "npm ci; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}",
    "npm run build; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}",
    "Push-Location src-tauri; cargo test; $cargo=$LASTEXITCODE; Pop-Location; if($cargo -ne 0){exit $cargo}",
    "& .\.agent\setup-android.ps1; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}",
    "npm run tauri -- android build --debug --apk --target aarch64; exit $LASTEXITCODE"
  ]
}
