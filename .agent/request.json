{
  "id": "check-gh-auth-20260826-0154-05",
  "enabled": true,
  "shell": "powershell",
  "cwd": ".",
  "timeout_seconds": 120,
  "commands": [
    "$env:Path = $env:Path + ';' + [Environment]::GetEnvironmentVariable('Path','User')",
    "Remove-Item Env:GITHUB_TOKEN -ErrorAction SilentlyContinue; Remove-Item Env:GH_TOKEN -ErrorAction SilentlyContinue",
    "gh auth status",
    "gh api user --jq .login"
  ]
}
