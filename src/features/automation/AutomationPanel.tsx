export function AutomationPanel() {
  return (
    <div className="sidebar-scroll">
      <div className="empty-panel">
        <span className="empty-symbol">◇</span>
        <strong>Automation reserved</strong>
        <p>This milestone only establishes the layout. Human-initiated generation remains the primary flow.</p>
      </div>
    </div>
  );
}
